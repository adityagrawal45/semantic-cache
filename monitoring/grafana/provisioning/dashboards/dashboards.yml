apiVersion: 1

providers:
  - name: "semantic-cache-dashboards"
    orgId: 1
    folder: "Semantic Cache"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
